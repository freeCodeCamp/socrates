librarian-v1.freecodecamp.org {
	# Enable access logs
	log {
		output file /var/log/caddy/librarian-access.log
		format json
	}

	# Enable compression
	encode gzip zstd

	# Reverse proxy all requests to the Express app
	reverse_proxy localhost:3000
}

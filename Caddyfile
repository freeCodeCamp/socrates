socrates.freecodecamp.dev {
	# Enable access logs
	log {
		output file /var/log/caddy/socrates-access.log
		format json
	}

	# Enable compression
	encode gzip zstd

	# Reverse proxy all requests to the Express app
	reverse_proxy localhost:3001
}